# سؤال‌های تست جدول جدید `demo_training_requests`

این سؤال‌ها برای تست این هستند که سیستم جدول جدید «درخواست‌های آموزشی» را از نظر معنایی درست فهمیده یا نه.

بعد از اجرای این مراحل:

```powershell
python scripts\simulate_schema_change.py add
python scripts\auto_update_semantic_layer.py --benchmark-limit 1
```

این سؤال‌ها را در UI بزن و در بخش «نمایش SQL تولید شده» چک کن که از جدول زیر استفاده شده باشد:

```sql
demo_training_requests
```

## تست‌های ساده

1. تعداد درخواست‌های آموزشی را بگو

انتظار:

- SQL باید `COUNT` داشته باشد.
- جدول باید `demo_training_requests` باشد.
- جواب درست با داده demo: `12`

2. اطلاعات درخواست‌های آموزشی را نشان بده

انتظار:

- همه ردیف‌های جدول demo نمایش داده شوند.
- ستون‌هایی مثل requester_name، request_type، province، status و estimated_cost دیده شوند.

3. نام درخواست‌دهنده‌های آموزشی را نشان بده

انتظار:

- ستون `requester_name` استفاده شود.

## فیلتر وضعیت

4. تعداد درخواست‌های آموزشی فعال را بگو

انتظار:

- فیلتر `status = 'active'`
- جواب درست با داده demo: `5`

5. درخواست‌های آموزشی تایید شده را نشان بده

انتظار:

- فیلتر `status = 'approved'`
- جواب درست با داده demo: 4 ردیف

6. درخواست‌های آموزشی در انتظار بررسی را نشان بده

انتظار:

- فیلتر `status = 'pending'`
- جواب درست با داده demo: 2 ردیف

## فیلتر استان و شهر

7. تعداد درخواست‌های آموزشی استان تهران را بگو

انتظار:

- فیلتر `province = 'تهران'`
- جواب درست با داده demo: `3`

8. درخواست‌های آموزشی شهر تهران را نشان بده

انتظار:

- فیلتر `city = 'تهران'`
- جواب درست با داده demo: 2 ردیف

9. درخواست‌های آموزشی استان اصفهان را نشان بده

انتظار:

- فیلتر `province = 'اصفهان'`
- جواب درست با داده demo: 2 ردیف

## فیلتر نوع درخواست

10. تعداد درخواست‌های کارگاه هوش مصنوعی را بگو

انتظار:

- فیلتر `request_type = 'کارگاه هوش مصنوعی'`
- جواب درست با داده demo: `3`

11. درخواست‌های دوره ضمن خدمت معلمان را نشان بده

انتظار:

- فیلتر `request_type = 'دوره ضمن خدمت معلمان'`
- جواب درست با داده demo: 2 ردیف

12. درخواست‌های دوره امور مالی را نشان بده

انتظار:

- فیلتر `request_type = 'دوره امور مالی'`
- جواب درست با داده demo: 2 ردیف

## فیلتر اولویت

13. تعداد درخواست‌های آموزشی با اولویت بالا را بگو

انتظار:

- فیلتر `priority = 'high'`
- جواب درست با داده demo: `5`

14. درخواست‌های آموزشی کم‌اولویت را نشان بده

انتظار:

- فیلتر `priority = 'low'`
- جواب درست با داده demo: 2 ردیف

## سؤال‌های ترکیبی

15. تعداد درخواست‌های آموزشی فعال استان تهران را بگو

انتظار:

- فیلتر ترکیبی `province = 'تهران'` و `status = 'active'`
- جواب درست با داده demo: `1`

16. درخواست‌های کارگاه هوش مصنوعی با اولویت بالا را نشان بده

انتظار:

- فیلتر ترکیبی `request_type = 'کارگاه هوش مصنوعی'` و `priority = 'high'`
- جواب درست با داده demo: 2 ردیف

17. درخواست‌های تایید شده مرکز فناوری آموزشی را نشان بده

انتظار:

- فیلتر `status = 'approved'`
- فیلتر `assigned_unit = 'مرکز فناوری آموزشی'`
- جواب درست با داده demo: 2 ردیف

18. درخواست‌های آموزشی استان تهران با هزینه بیشتر از ۱۰۰ میلیون را نشان بده

انتظار:

- فیلتر `province = 'تهران'`
- فیلتر `estimated_cost > 100000000`
- جواب درست با داده demo: 2 ردیف

## سؤال‌های رتبه‌بندی و عددی

19. گران‌ترین درخواست آموزشی کدام است؟

انتظار:

- مرتب‌سازی بر اساس `estimated_cost DESC`
- جواب درست: درخواست مهسا نادری / کارگاه هوش مصنوعی / 142,000,000

20. ارزان‌ترین درخواست آموزشی کدام است؟

انتظار:

- مرتب‌سازی بر اساس `estimated_cost ASC`
- جواب درست: درخواست نسرین هاشمی / دوره امور مالی / 22,000,000

21. میانگین هزینه درخواست‌های آموزشی را بگو

انتظار:

- استفاده از `AVG(estimated_cost)`
- جواب درست با داده demo: `70000000`

22. مجموع هزینه درخواست‌های آموزشی تایید شده را بگو

انتظار:

- فیلتر `status = 'approved'`
- استفاده از `SUM(estimated_cost)`
- جواب درست با داده demo: `335000000`

## چطور بفهمم semantic درست تشخیص داده؟

برای هر سؤال، سه چیز را چک کن:

1. آیا SQL از جدول `demo_training_requests` استفاده کرده؟
2. آیا ستون درست را انتخاب یا فیلتر کرده؟
3. آیا جواب عددی با مقدار مورد انتظار بالا یکی است؟

اگر SQL به جدول‌های قدیمی مثل `students`، `employees` یا `schools` رفت، یعنی سیستم معنی جدول جدید را خوب تشخیص نداده و باید alias/rule آن جدول تقویت شود.

# Layer B — Product Readiness Plan

هدف Layer B این است که سیستم از یک هسته‌ی قوی و قابل‌دمو، به یک محصول قابل نصب روی دیتابیس واقعی سازمان تبدیل شود؛ محصولی که اپراتور غیر برنامه‌نویس بتواند آن را به‌روزرسانی کند، مدیر بتواند سلامت آن را ببیند، و تیم فنی بتواند خطاها را سریع ریشه‌یابی کند.

## B1 — نقشه محصول و معیارهای پایان

وضعیت: انجام شد

خروجی این مرحله همین سند است. از این به بعد هر قدم B باید یکی از نیازهای محصولی زیر را پوشش دهد:

- نصب و راه‌اندازی روی دیتابیس جدید
- کشف خودکار schema و sample valueها
- ساخت/به‌روزرسانی semantic layer بدون نیاز به برنامه‌نویسی
- تست خودکار بعد از تغییر دیتابیس
- نمایش سلامت سیستم در UI
- audit، rollback و trace قابل اتکا
- امنیت داده و جلوگیری از خروج اطلاعات حساس
- آماده‌سازی برای دیتابیس واقعی کارفرما

## B2 — Onboarding checklist برای دیتابیس جدید

سیستم باید یک مسیر مشخص داشته باشد که وقتی دیتابیس جدید وصل شد، بداند چه چیزهایی را چک کند:

- اتصال PostgreSQL
- لیست جدول‌ها
- تعداد رکوردها
- primary keyها
- foreign keyها
- ستون‌های حساس مثل کد ملی، تلفن، آدرس، حقوق
- sample valueهای مهم
- جدول‌های بدون رابطه
- جدول‌های خیلی بزرگ
- ستون‌های مبهم یا تکراری بین چند جدول

معیار پایان:

- یک endpoint یا script بتواند گزارش onboarding تولید کند.
- گزارش برای انسان قابل خواندن باشد.
- ریسک‌ها را با سطح `ok`, `warning`, `blocked` نشان دهد.

## B3 — Schema quality gate

قبل از اینکه semantic layer فعال شود، schema باید از نظر کیفیت بررسی شود.

چک‌های پیشنهادی:

- جدول بدون primary key
- foreign key گمشده
- ستون‌های متنی بدون sample value
- جدول‌های بزرگ بدون index قابل استفاده
- ستون‌های حساس بدون طبقه‌بندی
- چند جدول با alias یا معنی مشابه

معیار پایان:

- اگر schema مشکل جدی داشت، سیستم به‌جای فعال‌سازی کورکورانه، پیام قابل فهم بدهد.

## B4 — Semantic confidence score

برای هر جدول و ستون، سیستم باید یک امتیاز اعتماد بسازد.

مثلاً:

- آیا اسم ستون واضح است؟
- آیا alias فارسی دارد؟
- آیا sample value کمک‌کننده دارد؟
- آیا رابطه‌ی جدول با بقیه schema مشخص است؟
- آیا قبلاً در benchmark درست جواب داده؟

معیار پایان:

- هر جدول/ستون یک confidence قابل مشاهده داشته باشد.
- ستون‌های کم‌اعتماد وارد clarification شوند، نه SQL اشتباه.

## B5 — Auto test generation برای جدول‌های جدید

وقتی جدول جدید اضافه شد، سیستم باید خودش چند سؤال تستی بسازد.

نمونه‌ها:

- تعداد رکوردهای جدول
- لیست چند رکورد اول
- فیلتر با sample value
- group by روی ستون categorical
- min/max روی ستون عددی
- فیلتر ترکیبی چندستونه

معیار پایان:

- بعد از schema discovery، مجموعه سؤال‌های smoke test ساخته شود.

## B6 — Benchmark runner برای دیتابیس جدید

سؤال‌های تولیدی B5 باید اجرا و نتیجه‌شان ذخیره شود.

معیار پایان:

- pass/fail هر سؤال مشخص باشد.
- SQL تولیدی ذخیره شود.
- علت شکست مشخص باشد: routing، ambiguity، validation، execution، یا answer formatting.

## B7 — Admin UI برای به‌روزرسانی semantic

اپراتور غیر برنامه‌نویس باید از UI بتواند:

- وضعیت schema را ببیند
- دکمه update را بزند
- نتیجه benchmark را ببیند
- اگر کیفیت پایین بود، فعال‌سازی را متوقف کند
- نسخه قبلی را rollback کند

معیار پایان:

- مسیر به‌روزرسانی semantic از UI قابل نمایش و قابل استفاده باشد.

## B8 — Human review loop

اگر سیستم درباره معنی یک ستون مطمئن نبود، باید پیشنهاد بدهد و انسان تأیید کند.

مثلاً:

```text
ستون requester_role احتمالاً یعنی پست/نقش درخواست‌دهنده.
آیا تأیید می‌کنید؟
```

معیار پایان:

- تأیید یا اصلاح انسان در semantic catalog ذخیره شود.

## B9 — Data sensitivity policy

سیستم باید بداند کدام داده‌ها حساس‌اند.

نمونه:

- کد ملی
- شماره تلفن
- حقوق
- اطلاعات بانکی
- آدرس
- اطلاعات پزشکی

معیار پایان:

- خروجی عمومی این ستون‌ها را پنهان کند.
- فقط اگر کاربر صریحاً خواست و policy اجازه داد، نمایش دهد.

## B10 — Explainability برای کارفرما

برای هر پاسخ، UI باید بتواند خلاصه کند:

- چرا این جدول انتخاب شد؟
- چرا این join استفاده شد؟
- چه فیلترهایی اعمال شد؟
- چرا query امن تشخیص داده شد؟

معیار پایان:

- در UI یک بخش قابل فهم برای مسیر تصمیم‌گیری وجود داشته باشد.

## B11 — Error taxonomy

خطاها باید دسته‌بندی شوند:

- سؤال نامفهوم
- ambiguity
- schema ناقص
- semantic layer قدیمی
- SQL نامعتبر
- execution timeout
- permission/security rejection
- answer formatting issue

معیار پایان:

- هر خطا code و پیام قابل فهم داشته باشد.

## B12 — Production runbook

یک راهنمای عملیاتی برای نصب و نگهداری سیستم لازم است.

باید شامل این موارد باشد:

- اجرا
- توقف
- restart
- اتصال دیتابیس
- sync schema
- update semantic
- benchmark
- rollback
- مشاهده audit
- رفع خطاهای رایج

معیار پایان:

- یک فایل راهنما برای تیم فنی/اپراتور آماده شود.

وضعیت: انجام شد

خروجی:

- `PRODUCTION_RUNBOOK.md`

## ترتیب پیشنهادی اجرا

1. B2 — Onboarding checklist
2. B3 — Schema quality gate
3. B4 — Semantic confidence score
4. B5 — Auto test generation
5. B6 — Benchmark runner
6. B7 — Admin UI
7. B8 — Human review loop
8. B9 — Data sensitivity policy
9. B10 — Explainability
10. B11 — Error taxonomy
11. B12 — Production runbook

## تعریف پایان Layer B

Layer B وقتی کامل است که بتوانیم یک دیتابیس جدید را وصل کنیم و بدون برنامه‌نویسی دستی:

1. schema را کشف کنیم،
2. کیفیت آن را بسنجیم،
3. semantic layer بسازیم،
4. تست خودکار بگیریم،
5. فقط اگر کیفیت قابل قبول بود فعال کنیم،
6. در UI وضعیت را ببینیم،
7. در صورت خطا rollback کنیم،
8. و برای کارفرما توضیح دهیم سیستم چرا به این جواب رسیده است.
